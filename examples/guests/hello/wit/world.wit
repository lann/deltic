package polyengine:hello;

/// Smallest possible guest: one exported function, string in, string out.
/// First target for the host's lift/lower of strings (utf8, realloc path).
world hello {
  export greet: func(name: string) -> string;
}
